services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.prod
    networks:
      - para-network

networks:
  para-network:
    driver: bridge
